# Database
MYSQL_ROOT_PASSWORD=root123
MYSQL_DATABASE=bankdb
MYSQL_USER=bankuser
MYSQL_PASSWORD=bankpass

# App – these will be passed to the Spring Boot container
# (Note: the compose file maps SPRING_DATASOURCE_* to these)
# The compose already uses the variables above, so no extra needed.